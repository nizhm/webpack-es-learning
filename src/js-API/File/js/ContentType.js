const ContentTypes = [
  'text/plain',
  'text/html', // preflight `OPTIONS` request needed
  'multipart/form-data',
  'application/x-www-form-urlencoded',
  'application/octet-stream' // preflight `OPTIONS` request needed
]

export default ContentTypes
